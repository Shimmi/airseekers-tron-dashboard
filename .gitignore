# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env / secrets — never commit real tokens; only the template is tracked.
# Dev token lives in .env.development.local, deploy token in .env.production.local
# (both match *.local above; listed explicitly so a plain .env.* can't slip in).
.env
.env.*
!.env.example

# Playwright
test-results/
playwright-report/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
